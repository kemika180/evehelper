# Python
__pycache__/
*.py[cod]
*.so
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
*.egg-info/

# Local cache: SDE, token store, captured character data — never commit
/data/
*.token
.env

# Exported crafting recipes (written to the working dir by the Crafting popup)
*-recipe.md

# Local agent/dev-process guidance — kept locally, never committed to the remote
CLAUDE.md
WORKFLOW.md
# Internal design/roadmap doc — kept local, not shared in the repo
ARCHITECTURE.md
