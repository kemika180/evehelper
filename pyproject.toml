[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "evehelper"
version = "0.1.0"
description = "A TUI money-making advisor for EVE Online (ESI market data + character state)."
requires-python = ">=3.12"
dependencies = [
    "polars>=1.42",
    "pydantic>=2.11",
    "httpx>=0.28",
    "textual>=8.0",
    "textual-plotext>=1.0",
    "keyring>=25.0",
]

[project.scripts]
evehelper = "evehelper.cli:main"

[dependency-groups]
dev = [
    "mypy>=1.15",
    "pytest>=8.3",
    "ruff>=0.15",
    "import-linter>=2.13",
]

[tool.hatch.build.targets.wheel]
packages = ["python/evehelper"]

[tool.pytest.ini_options]
testpaths = ["tests/python"]

# The non-negotiable pure-core boundary, enforced mechanically: the analysis
# core (market/, advisor/) is deterministic and must never reach the I/O shell
# (esi/, data/) or the network.
[tool.importlinter]
root_package = "evehelper"
include_external_packages = true

[[tool.importlinter.contracts]]
name = "Pure analysis core never imports the I/O shell or network libraries"
type = "forbidden"
source_modules = ["evehelper.market", "evehelper.advisor"]
forbidden_modules = ["evehelper.esi", "evehelper.data", "httpx"]
